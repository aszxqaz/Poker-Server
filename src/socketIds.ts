export const socketIdsMap = new Map()

// export function setSocketId(username: string, socketId: string) {
//   socketIdsMap.set(username, socketId)
// }

// export function getSocketId(username: string): string {
//   return socketIdsMap.get(username)
// }